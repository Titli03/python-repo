def main():
    #string variable
    instance_type='t2micro'
    message="i have"
    
    #int variable
    num=2
    hours=5
    print(f"{message} {num} ec2 running for {hours}.")
    
    #boolian type
    instance_running=True
    #print(f"my instances are running {instance_running}")
    #print(f"data type for instance running is  {type(instance_running)}")
    
    #floating data type
    floate=5.2
    print(f"my {instance_type} is running for {hours} with {floate} USD..")
    











if __name__ == '__main__':
    main()